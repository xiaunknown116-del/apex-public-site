/* Apex Capital — analytics stub (no third-party tracking on public site) */
(function () {
  "use strict";
  // Intentionally empty: public site does not load external analytics.
  // Replace only after compliance review.
})();
