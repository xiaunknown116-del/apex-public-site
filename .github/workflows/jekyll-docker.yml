# Disabled — this repo is static HTML for Cloudflare Pages, not Jekyll.
# Deploy path: Cloudflare Pages → Connect Git → build command empty → output /
name: jekyll-docker (disabled)
on:
  workflow_dispatch:
jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Jekyll workflow disabled. Use Cloudflare Pages."
